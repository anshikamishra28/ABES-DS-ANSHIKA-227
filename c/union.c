#include<stdio.h>
int main(){
    int n,m;
    printf("Enter the size of array :");
    scanf("%d",&n);
    printf("Enter the size of array :");
    scanf("%d",&m);
    int i,j,k=0;
    int arr[n],brr[m],uni[m+n];
    for(i=0;i<n;i++){
        uni[k++]=arr[i];
    }
    for(i=0;i<n;i++){
        int found=0;
        for(j=0;j<m;j++){
            if(brr[])
        }
    }
    

    return 0;
}