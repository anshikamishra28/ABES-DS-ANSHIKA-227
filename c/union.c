#include<stdio.h>
int main(){
    int n,m;
    printf("Enter the size of array :");
    scanf("%d",&n);
    printf("Enter the size of array :");
    scanf("%d",&m);
    int arr[n];brr[m];
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            
        }
    }

    return 0;
}