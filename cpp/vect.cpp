#include<iostream>
#include<bits/stdc++.h>
#include<algorithm>
using namespace std;
int main(){
    int value;
    vector<int>v;
    bool binary_search(v.begin(),v.end(),value)
    while(true){
        cin>>value;
        if(value==-1)
        break;
        else
        v.push_back(value);
    }
    
}